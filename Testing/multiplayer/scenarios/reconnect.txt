# Dedicated-server side of the reconnect scenario. Purely an observer, like basic-round.txt -
# a real UNITY_SERVER build has no ClientManager connection of its own. The client embarks,
# disconnects, and reconnects with the same ckey; this just needs to stay up long enough for
# that whole round trip (see reconnect-client.txt) before tearing down.
wait_connected
wait_round Ongoing
wait_seconds 60
disconnect
