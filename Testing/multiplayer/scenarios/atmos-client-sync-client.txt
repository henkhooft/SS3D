# Client side of the atmos-client-sync scenario
# (Documents/architecture/2026-07_atmos-client-visualization-sync.md). Verifies a real pure
# client — no AtmosSimulation of its own — actually receives and applies atmos chunk patches
# broadcast by the server, instead of only checking an RPC round trip survives (like
# basic-round's `console playerlist`).
#
# atmosdebug forces a fresh dirty chunk at the caller's own tile so the check doesn't depend on
# the map's initial gas state having already settled by the time this client connects — Phase 1
# has no late-join bootstrap, so a chunk that went dirty before this client joined would not be
# resent until it changes again (see the doc's "Documented fork deviation" section).
wait_connected
ready
start_round
wait_round Ongoing
embark
wait_seconds 1
console atmosdebug heat 500
wait_seconds 2
console atmosclientstatus assert
wait_seconds 1
disconnect
