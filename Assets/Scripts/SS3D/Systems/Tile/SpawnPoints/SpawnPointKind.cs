namespace SS3D.Systems.Tile.SpawnPoints
{
    /// <summary>
    /// Whether a spawn marker is tagged to a crew job or an antagonist category.
    /// </summary>
    public enum SpawnPointKind : byte
    {
        Job = 0,
        Antagonist = 1,
    }
}
