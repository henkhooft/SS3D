namespace SS3D.Systems.Tile.MapEditor
{
    /// <summary>
    /// Placement mode for map editor commands. v1 always uses Normal; CreativeInstant skips construction ladder.
    /// </summary>
    public enum MapEditorPlacementMode
    {
        Normal,
        CreativeInstant,
    }
}
