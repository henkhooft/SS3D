namespace SS3D.UI.Lobby
{
    /// <summary>In-memory Character Creator draft for Phase E1 (local only, no network).</summary>
    public sealed class CharacterCreatorDraft
    {
        public string Name = LobbyMockData.CharacterName;
    }
}
